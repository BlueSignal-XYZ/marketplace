// /workspaces/Marketplace/src/components/Navbar.jsx

/**
 * Legacy Navbar (deprecated)
 *
 * We now use:
 *  - MarketplaceHeader  (marketplace mode)
 *  - CloudHeader        (cloud mode) + Sidebar
 *
 * This stub keeps old imports of <Navbar /> from breaking,
 * but intentionally renders nothing so we don't get a second header.
 */

export default function Navbar() {
  return null;
}