# BlueSignal WQM-1 Firmware Dependencies
# Pin all versions for reproducible builds

smbus2==0.4.3              # I2C bus (ADS1115 ADCs)
spidev==3.6                # SPI bus (SX1262 LoRa radio)
RPi.GPIO==0.7.1            # GPIO control (relay, LED, radio pins)
pyyaml==6.0.1              # Configuration files
pynmea2==1.19.0            # NMEA sentence parsing (GPS)
